apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: part-two-routing
spec:
  rules:
  - host: app2.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: app-two-service
            port:
              number: 80
  - http:
      paths:
      - pathType: Prefix
        path: "/"
        backend:
          service:
            name: app-three-service
            port:
              number: 80
